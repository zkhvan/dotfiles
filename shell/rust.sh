# shell/rust.sh

export KZ_SOURCE="${KZ_SOURCE} -> shell/rust.sh {"

# ============================================================================
# Add the cargo binaries to the PATH
# ============================================================================
PATH="${HOME}/.cargo/bin:${PATH}"

KZ_SOURCE="${KZ_SOURCE} }"
