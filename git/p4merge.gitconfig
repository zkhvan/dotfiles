[diff]
	tool = p4merge

[difftool "p4merge"]
	cmd = p4merge \"$LOCAL\" \"$REMOTE\"

[mergetool "p4merge"]
	cmd = p4merge \"$BASE\" \"$LOCAL\" \"$REMOTE\" \"$MERGED\"

[merge]
	tool = p4merge

# vim: ft=gitconfig :
